# Frontend (Vite). Optional: base URL of the backend when not same-origin.
# In dev with Vite proxy, leave empty. Otherwise set e.g. http://localhost:3001
VITE_API_URL=

# Gemini (frontend build injects this)
GEMINI_API_KEY=your_gemini_api_key

# --- Server (TrackAdobe authenticated proxy). Copy to .env or server/.env ---
# Option A: full Cookie header string (paste from browser after logging into trackadobestock.com)
# TRACK_ADOBE_COOKIES=__stripe_mid=...; __Host-next-auth.csrf-token=...; __Secure-next-auth.callback-url=...; __Secure-next-auth.session-token=...

# Option B: individual vars (server builds Cookie header)
TRACK_ADOBE_SESSION_TOKEN=
TRACK_ADOBE_CSRF_TOKEN=
TRACK_ADOBE_CALLBACK_URL=https%3A%2F%2Ftrackadobestock.com
TRACK_ADOBE_STRIPE_MID=
